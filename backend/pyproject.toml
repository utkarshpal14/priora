[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B008"]  # Allow FastAPI Depends in function defaults if used directly

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
